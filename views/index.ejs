<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= title %></title>
    <link rel="stylesheet" href="/style.css" />
</head>
<body>
    <div class="container">
        <h1><%= title %></h1>
        <p>Welcome to your first Express + EJS app with CSS!</p>

        <form action="/add" method="POST">
            <input type="text" name="name" placeholder="Enter your name" required />
            <button type="submit">Add Name</button>
        </form>

        <h2>All Names</h2>
        <ul>
            <% users.forEach(user => { %>
                <li><%= user.name %></li>
            <% }) %>
        </ul>

        <p>add for checking new comments</p>
    </div>
</body>
</html>
